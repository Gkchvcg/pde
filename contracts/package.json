{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "HARDHAT_DISABLE_EDR=1 hardhat compile",
    "test": "HARDHAT_DISABLE_EDR=1 hardhat test",
    "node": "HARDHAT_DISABLE_EDR=1 hardhat node",
    "deploy:local": "HARDHAT_DISABLE_EDR=1 hardhat run scripts/deploy.js --network localhost",
    "deploy:amoy": "HARDHAT_DISABLE_EDR=1 hardhat run scripts/deploy.js --network polygon_amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "ethers": "^6.16.0",
    "hardhat": "2.19.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
